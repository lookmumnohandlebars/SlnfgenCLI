name: ⛙ Merge To Main

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    name: "🧪 Build & Test"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout

      - name: Build and Test
        uses: ./.github/actions/hello-world-composite-action
